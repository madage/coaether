# Server
SERVER_PORT=8088

# PostgreSQL
POSTGRES_DSN=postgres://myai:myai123@localhost:5432/myai?sslmode=disable

# Redis
REDIS_ADDR=localhost:6379
REDIS_PASS=

# JWT Secret (change this in production)
JWT_SECRET=superco-secret-key

# SMTP (for email invitations — optional, links are logged if not configured)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@superco.local

# Public URL (used in invitation links)
PUBLIC_URL=http://localhost:5173
